A simple Qt5 / Qt4 plugin (an amplifier with a Qt5 / Qt4 GUI or a ttl GUI).

To build the plugin:
```
$ mkdir build
$ cd build
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
$ sudo make install
```

To build the Qt5 version of the plugin:
```
$ mkdir build
$ cd build
$ cmake -DCMAKE_INSTALL_PREFIX=/usr -DENABLE_QT5=ON ..
$ make
$ sudo make install
```

To load the plugin:
```
$ jalv.qt https://github.com/ycollet/qamp
or
$ jalv.qt4 https://github.com/ycollet/qamp
or
$ jalv.qt5 https://github.com/ycollet/qamp
```
